# Project.R 웹 사용법

## 구성
- `test/index.html` — 배포용 테스트 웹 (관람객이 직접 하는 퀴즈)
- `archive/index.html` — 아카이브 전시 웹 (누적된 기록을 보여주는 "기록의 벽")
- `list/index.html` — 전체 영웅 리스트 웹 (49개 시리즈 × 가로 스크롤, 310명 전체 열람)
- `images/heroes/` — 리스트 웹에서 쓸 전사 이미지를 넣는 곳 (아래 "이미지 넣는 법" 참고)
- `data/rangers.json`, `data/keywords.json`, `data/rangers_by_series.json` — 원본 데이터 (각 웹 안에 이미 내장되어 있어서 실제로는 안 열어봐도 됨)

세 웹 다 인터넷 연결 없이도 그 자체로 완전히 작동한다. 캐릭터 데이터(310명)와 키워드가 파일 안에 전부 들어있다.

세 웹은 서로 링크로 연결되어 있다: 테스트 웹의 첫 화면과 마지막 화면, 아카이브 웹에 "전체 영웅 리스트 보기" 링크가 있고, 리스트 웹에도 테스트/아카이브로 돌아가는 링크가 있다. (모두 새 탭으로 열림)

## 리스트 웹에 이미지 넣는 법

리스트 웹은 지금 이미지 없이 색상 블록 + 이름으로 표시된다. 나중에 실제 이미지를 넣으려면 `images/heroes/` 폴더에 아래 규칙으로 파일명을 맞춰서 넣기만 하면 자동으로 반영된다 (코드 수정 불필요):

```
images/heroes/{시리즈번호 2자리}_{전사명(띄어쓰기 제거)}.jpg
```

예: 1번 시리즈 아카 레인저 → `images/heroes/01_아카레인저.jpg`

정확한 파일명 목록이 필요하면 `data/rangers_by_series.json`을 열어서 각 캐릭터의 `imageSlug` 값을 확인하면 된다 (`imageSlug + ".jpg"`가 파일명).

## 중요 — 꼭 이렇게 열어야 함

두 파일을 그냥 더블클릭해서 열면(`file://` 주소) 브라우저에 따라 "기록 보관하기"로 저장한 내용이 아카이브 웹에서 안 보일 수 있다. 이건 브라우저가 로컬 파일을 서로 다른 사이트로 취급해서 저장공간을 공유하지 않기 때문.

**전시장에서 실제로 쓸 때는 반드시 로컬 서버로 열어야 한다.** (Q6 AI 해석을 연결한 뒤로는 `python3 -m http.server` 대신
아래 `server.py`로 열어야 한다 — 정적 파일 서빙은 그대로 하면서 AI 해석 API도 같이 떠 있어야 하기 때문.)

1. 이 폴더(ProjectS) 전체를 노트북/키오스크에 옮긴다.
2. 처음 한 번만: 터미널에서 이 폴더로 이동해서 필요한 패키지 설치
   ```
   pip3 install -r requirements.txt --break-system-packages
   ```
3. `.env.example`을 복사해서 같은 폴더에 `.env`로 저장하고, 그 안에 `ANTHROPIC_API_KEY=...` 값을 실제 발급받은 키로 채운다.
4. 터미널에서 이 폴더로 이동한 뒤 서버 실행:
   ```
   python3 server.py
   ```
5. 브라우저에서 접속:
   - 실제 사용 중인 퀴즈 웹(사진 연결·AI 해석 포함): `http://localhost:8000/redesign/test/`
   - 아카이브 웹: `http://localhost:8000/archive/`
   - (구버전, 참고용) 리스트 웹: `http://localhost:8000/list/`, 예전 테스트 웹: `http://localhost:8000/test/`

이렇게 같은 주소(localhost:8000) 아래에서 열면 퀴즈 웹에서 저장한 기록이 아카이브 웹에도 정상적으로 쌓인다.
`.env`에 키를 안 넣어도 사이트 전체(사진·모자이크·아카이브·지구본)는 평소대로 잘 동작한다 — Q6의 AI 한 줄 해석만
조용히 비어있는 채로 넘어간다(전시 중 인터넷이 끊겨도 퀴즈가 멈추지 않도록 일부러 이렇게 만들었다).

## 여러 대의 기기(태블릿 등)에서 동시에 운영할 경우

localStorage는 기기/브라우저별로 따로 저장된다. 즉 태블릿 3대로 테스트를 받으면 기록도 3곳에 따로 쌓인다. 아카이브 웹의 "기록 가져오기(JSON)" 버튼으로 각 기기의 "기록 내보내기(JSON)" 파일을 하나의 아카이브 웹에 모아 합칠 수 있다.

## AI 주관식 해석에 대해

`redesign/test/`(실제 사용 중인 버전)는 Q6 주관식 답변을 `server.py`를 통해 Claude API로 보내서 실제 AI가 쓴
한 줄 해석을 받아온다 (`redesign/test/script.js`의 `fetchAiInterpretation()` → `server.py`의 `/api/interpret`).
API 키는 서버(`.env`)에만 있고 브라우저/사이트 코드에는 전혀 노출되지 않는다. 서버가 꺼져있거나 인터넷이
안 되거나 `.env`에 키가 없으면, 이 한 줄만 조용히 비어있는 채로 넘어가고 퀴즈 자체는 정상 진행된다.

(예전 `test/` 폴더는 규칙 기반 문장 치환 방식이었다 — `"입력한 문장" — 그 이야기는, 멸종된 게 아니라 아직 완성되지 않은 것입니다`.
지금은 쓰지 않는 구버전이라 참고용으로만 남겨둠.)

## 데이터가 궁금할 때

`약점성격_키워드_정리_및_5문항설계.md`, `약점성격_키워드_매핑.csv`에 이 웹이 사용하는 87개 대표 키워드와 5문항 분류 근거가 정리되어 있다.
