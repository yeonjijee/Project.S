# Project.S 버셀(Vercel) 배포하기

이 문서는 GitHub 저장소를 만들어 Vercel에 연결하는 방식으로 배포하는 순서다. 배포 준비 파일은
이미 다 만들어져 있다:

- `api/interpret.py` — 로컬 `server.py`의 `/api/interpret`(Q6 AI 해석)와 똑같은 로직을 Vercel이
  실행할 수 있는 서버리스 함수 형태로 옮겨놓은 것. `server.py`는 로컬 개발용으로 그대로 둔다.
- `api/requirements.txt` — 그 함수가 필요로 하는 파이썬 패키지(`anthropic`) 목록.
- `vercel.json` — 함수 실행 제한 시간(30초) 설정.
- `.gitignore` — `.env`(API 키), `__pycache__`, `.DS_Store` 등을 깃에 올리지 않게 막아둠.

이 아래 단계는 **직접** 터미널에서 실행해야 한다 — 계정 로그인이 필요한 부분이라 내가 대신 해줄
수 없다.

## 1. 로컬에서 git 저장소 만들기

터미널에서:

```
cd /Users/jeeyeonji/Desktop/hero/0803/ProjectS
rm -rf .git          # 혹시 남아있을 수 있는 깨진 .git 폴더 정리(있으면 지우고, 없으면 그냥 넘어가도 됨)
git init
git add -A
git commit -m "Project.S 첫 배포"
```

## 2. GitHub에 저장소 만들고 올리기

1. github.com에서 새 저장소(Repository)를 하나 만든다 — Public이든 Private이든 상관없음, README
   추가 없이 빈 저장소로 만든다.
2. 만들고 나면 GitHub이 알려주는 주소(`https://github.com/아이디/저장소이름.git`)를 복사해서:

```
git remote add origin 여기에_복사한_주소_붙여넣기
git branch -M main
git push -u origin main
```

## 3. Vercel에서 저장소 연결하기

1. vercel.com에 로그인(없으면 GitHub 계정으로 가입 가능).
2. "Add New… → Project" → 방금 만든 GitHub 저장소 선택 → Import.
3. Framework Preset은 그냥 "Other"(또는 자동 감지되는 대로) 두면 된다 — 특별한 빌드 설정 필요 없음
   (정적 파일 + `api/` 폴더 구조라 Vercel이 자동으로 인식함).
4. **배포하기 전에** "Environment Variables"에서 키를 하나 추가한다:
   - Name: `ANTHROPIC_API_KEY`
   - Value: (로컬 `.env` 파일에 있는 실제 키 값 — Vercel 대시보드에만 입력하고, 나한테는 절대
     붙여넣지 않아도 됨)
5. Deploy 버튼 클릭. 1~2분 안에 끝나고 `프로젝트이름.vercel.app` 같은 주소가 생긴다.

## 4. 확인

배포된 주소에서 `/redesign/test/index.html`로 들어가서 카메라 켜기부터 결과 화면까지 한 번
쭉 테스트해보면 된다. Q6에서 "수호 영웅의 답변을 생성하는 중…"이 뜨고 실제 AI 해설이 나오면
API 키까지 정상 연결된 것.

이후에 코드를 더 고치면, 같은 폴더에서 `git add -A && git commit -m "..." && git push`만
하면 Vercel이 자동으로 다시 배포해준다(GitHub 연동의 장점).

## 참고

- `server.py`(로컬 개발용, `python3 server.py`)는 그대로 남아있다 — 로컬에서 테스트할 땐 계속
  이걸 쓰면 된다. Vercel은 `api/interpret.py`를 대신 쓴다.
- `.env` 파일은 `.gitignore`에 들어있어서 깃에 올라가지 않는다. 그래서 Vercel에는 3-4단계처럼
  대시보드에서 따로 키를 넣어줘야 한다.
